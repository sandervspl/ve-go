import theme from '../../style/theme';

export const listItemBgColor = {
  default: theme.color.white,
  pressed: theme.color.green,
};

export const listItemBorderColor = {
  default: theme.color.lightgray,
  pressed: theme.color.green,
};

export const listItemTextColor = {
  default: theme.color.black,
  pressed: theme.color.white,
  light: theme.color.lightgray,
};

export const listItemTitleColor = {
  default: theme.color.green,
  pressed: theme.color.white,
};
